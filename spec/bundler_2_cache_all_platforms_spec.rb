# frozen_string_literal: true

RSpec.describe Bundler2CacheAllPlatforms do
  it "has a version number" do
    expect(Bundler2CacheAllPlatforms::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
